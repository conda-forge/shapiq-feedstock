schema_version: 1

context:
  name: shapiq
  version: 1.5.0

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/shapiq-${{ version }}.tar.gz
  sha256: 4848ff7d540eab4f56b6b01b7ce22b027282f526dbe4d69cf9766ea3765231ba

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=61.0
    - pip
  run:
    - python >=${{ python_min }}
    - numpy
    - scipy
    - pandas
    - scikit-learn
    - tqdm
    - requests
    - matplotlib-base
    - networkx
    - colour
    - pillow
    - joblib
tests:
  - python:
      imports:
        - shapiq
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: Shapley Interactions for Machine Learning
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/mmschlk/shapiq

extra:
  recipe-maintainers:
    - janjagusch
