schema_version: 1

context:
  name: shapiq
  version: "1.7.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/shapiq-${{ version }}.tar.gz
  sha256: 17cad036c50ec5782a4e72550ba5b1e73955b8f06387acb6e20d436eb33eb44d

build:
  number: 0
  skip: match(python, "<3.12")
  script: |
    export LIBOMP_PREFIX=$PREFIX
    ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ compiler('cxx') }}
    - ${{ stdlib('c') }}
  host:
    - python
    - setuptools >=69
    - setuptools-scm >=8
    - wheel
    - numpy
    - pip
    - if: osx
      then:
        - llvm-openmp
  run:
    - python
    - numpy
    - scipy
    - pandas
    - scikit-learn
    - tqdm
    - requests
    - matplotlib-base
    - networkx
    - colour
    - pillow
    - joblib

tests:
  - python:
      imports:
        - shapiq
      pip_check: true

about:
  summary: Shapley Interactions for Machine Learning
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/mmschlk/shapiq

extra:
  recipe-maintainers:
    - janjagusch
